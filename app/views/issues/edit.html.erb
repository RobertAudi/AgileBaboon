<div class="row-fluid">
  <div class="span12">
    <%= simple_form_for @issue, url: project_issue_path(params[:project_id], params[:id]) do |f| %>
      <%= render 'form', f: f %>
    <% end %>
  </div>
</div>
