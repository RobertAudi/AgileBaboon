<p style="margin-bottom: 20px;"><%= link_to "Create new user", new_kong_user_path %></p>
<table>
  <tr>
    <th>Username</th>
    <th>Email</th>
  </tr>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.username %></td>
      <td><%= user.email %></td>
    </tr>
  <% end %>
</table>

<%= paginate @users %>
